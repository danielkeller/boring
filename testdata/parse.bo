fn foo['a, T](bar: &'a T, i: bool) -> i32 {
    let baf = ();
    if i {
        let a = baf(bar);
        if a {
            let baz = ();
            baz(baf);
        }
        a()
    } else {
        ()
    }
}